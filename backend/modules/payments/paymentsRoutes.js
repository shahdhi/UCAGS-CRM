const express = require('express');
const router = express.Router();

/**
 * /api/payments/* -- MIGRATED to Supabase Edge Function: payments
 * Routes moved to: supabase/functions/payments/index.ts
 * Clients route via apiService.js (EDGE_BASE_PAYMENTS)
 */

module.exports = router;


Here’s what I found and what I’ll do:

Current State
The leads page already uses Supabase as its data layer — the crm_leads table is the primary store. The frontend calls /api/crm-leads/* which currently goes through the Vercel serverless Express backend (api/[...path].js → backend/modules/crmLeads/crmLeadsRoutes.js → crmLeadsService.js).

The goal is to move those same routes into Supabase Edge Functions so the frontend calls the Supabase edge URL directly, removing Vercel from the leads flow entirely.

What I’ll Build
1. supabase/functions/crm-leads/index.ts
A single Deno edge function that handles all the existing /api/crm-leads/* routes:

GET /crm-leads/admin — admin list leads
GET /crm-leads/my — officer list their leads
PUT /crm-leads/admin/:batch/:sheet/:id — admin update lead
PUT /crm-leads/my/:batch/:sheet/:id — officer update lead
POST /crm-leads/admin/create — admin create lead
POST /crm-leads/my/create — officer create lead
POST /crm-leads/admin/copy, copy-bulk — copy leads
POST /crm-leads/admin/distribute-unassigned, bulk-assign, bulk-distribute, bulk-delete — bulk ops
GET /crm-leads/admin/export.csv — CSV export
POST /crm-leads/admin/import — CSV import
GET /crm-leads/meta/sheets, admin/meta/sheets, admin/meta/batches — metadata
All using the Supabase service role key already available in edge runtime as Deno.env.get('SUPABASE_SERVICE_ROLE_KEY').

2. supabase/functions/_shared/cors.ts
Shared CORS headers helper.

3. Update public/frontend/services/apiService.js
Change fetchAPI() so calls to /crm-leads/* go to the Supabase edge function URL (https://xddaxiwyszynjyrizkmc.supabase.co/functions/v1/crm-leads/*) instead of /api/crm-leads/*.

4. Clean up unnecessary files
backend/modules/leads/leadsRoutes.js + leadsService.js (old Google Sheets leads — superseded by crm-leads)
backend/modules/leads/userLeadsRoutes.js + userLeadsService.js (old officer sheets — superseded)
Multiple receiptsRoutes_v*.js old versions
UCAGS-CRM-main/ duplicate folder (entire copy of the project sitting in the root)
Various .md analysis files

Plan
Strategy: Direct Supabase Edge URL for /crm-leads/*
The frontend’s apiService.js currently calls /api/crm-leads/* which hits Vercel → Express → Supabase. We cut out Vercel entirely for these routes by pointing the frontend directly at the Supabase edge function.

Changes I’ll Make
1. Create supabase/functions/crm-leads/index.ts
Port all the logic from crmLeadsRoutes.js + crmLeadsService.js into a single Deno edge function. The Supabase client is available natively in edge functions so no cold-start npm issues.

2. Create supabase/functions/_shared/cors.ts
Shared CORS helper for edge functions.

3. Update public/frontend/services/apiService.js
Change only the /crm-leads calls to hit https://xddaxiwyszynjyrizkmc.supabase.co/functions/v1/crm-leads directly. All other routes (/api/...) stay unchanged on Vercel.

4. Clean up old/unnecessary files:

Multiple stale receipt route versions (receiptsRoutes_v3 through v11)
Old Google-Sheets leads service files (superseded by crm-leads/Supabase)
Duplicate .md analysis files at root level
The UCAGS-CRM-main/ duplicate folder